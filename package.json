{
  "name": "mobile-chromium-ui",
  "version": "0.1.0",
  "description": "Mobile-first Chromium browser UI for Linux",
  "private": true,
  "type": "module",
  "author": "Terry",
  "license": "MIT",

  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "backend": "node backend/server.js",
    "start": "node backend/server.js",

    "install-browser": "bash backend/scripts/install.sh",
    "update-browser": "bash backend/scripts/update.sh",
    "first-run": "bash backend/scripts/first-run.sh"
  },

  "dependencies": {
    "svelte": "^5.38.0"
  },

  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.3",
    "vite": "^7.1.0"
  }
}
